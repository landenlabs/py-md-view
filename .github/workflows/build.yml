name: Build and Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            artifact: md-view-macos.zip
            onefile_flag: ""
            icon: icon.icns
            add_data_sep: ":"
          - os: windows-latest
            artifact: md-view.exe
            onefile_flag: "--onefile"
            icon: icon.ico
            add_data_sep: ";"

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt pyinstaller

      - name: Build
        run: >-
          pyinstaller ${{ matrix.onefile_flag }} --windowed --name md-view
          --icon "${{ matrix.icon }}"
          --add-data "icon.png${{ matrix.add_data_sep }}."
          --add-data "mermaid.min.js${{ matrix.add_data_sep }}."
          md-view.py

      # macOS --windowed always builds a .app bundle (onedir under the hood);
      # --onefile there is deprecated (PyInstaller warns) and produces a bare
      # executable with no Info.plist, so the Dock icon reverts once quit.
      - name: Zip macOS app bundle
        if: runner.os == 'macOS'
        run: ditto -c -k --sequesterRsrc --keepParent "dist/md-view.app" "dist/md-view-macos.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: md-view-${{ matrix.os }}
          path: dist/${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/md-view-macos-latest/md-view-macos.zip
            artifacts/md-view-windows-latest/md-view.exe
